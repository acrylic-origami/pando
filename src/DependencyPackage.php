<?hh // strict
namespace Shufflr;
type DependencyPackage = shape();
// This will eventually store the common properties between depencency packages. So far, there are none, so it's mostly a naming thing